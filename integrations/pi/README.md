# AgentUse for Pi

Turn the workflow in the current Pi conversation into a focused, validated
AgentUse agent with `/automate`.

For local development, build the integrations and install the generated Pi
package:

```sh
pi install ./dist/integrations/pi
```

For distribution, publish the generated
`dist/integrations/artifacts/agentuse-pi-package.tgz` package to npm.
